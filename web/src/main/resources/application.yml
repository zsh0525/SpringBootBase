server:
  port: 8008 # 端口号

spring:
  profiles:
    include: dev

# swagger 相关配置
swagger:
  enable: true
  path: /
  basePackage: com.zsh.www.controller
  title: springBootBaseApi
  version: 0.0.1
  contact: zsh,www.zsh.com,479113908@qq.com
  contact3:
    - zsh3
    - www.zsh.com
    - 479113908@qq.com
  description: Swagger测试Demo


log:
  name: webLog
  # 日志文件所在目录，注意本地和部署环境。若用命令执行的，可在启动时赋值 ，如：--log.path = XXXXX
  path: D:/

logging:
  # 自定义配置文件路径。
  config: classpath:log-config/logback-spring.xml


